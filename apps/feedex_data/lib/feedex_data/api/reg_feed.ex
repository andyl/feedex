defmodule FeedexData.Api.RegFeed do
  @moduledoc """
  Utilities for working with RegFeeds
  """

  # alias FeedexData.Ctx.Account.Folder
  # alias FeedexData.Ctx.Account.Register
  # alias FeedexData.Ctx.News.Feed
  # alias FeedexData.Repo
  # import Ecto.Query

  def create(_user_id, _folder_id, _feed_name, _feed_url) do
    :ok
  end

end
