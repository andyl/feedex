name: CD_FEEDEX
on:
  push:
    branches: 
      - master
  workflow_dispatch:
jobs:
  ci:
    uses: ./.github/workflows/base_ci.yml
  cd: 
    runs-on: self-hosted
    needs: ci 
    name: Build and Deploy 
    env: 
      MIX_ENV: prod
      ImageOS: ubuntu22
    steps: 
      - name: deploy script absolute path 
        run: /home/aleak/script/test.sh
      - name: deploy script relative path 
        run: ~/script/test.sh

